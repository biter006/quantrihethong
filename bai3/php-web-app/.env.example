# Chép file này thành .env rồi đổi giá trị:  cp .env.example .env
# .env chứa thông tin nhạy cảm — KHÔNG commit lên Git (đã có trong .gitignore)

# ---------- MySQL ----------
MYSQL_ROOT_PASSWORD=Password123
MYSQL_DATABASE=university

# ---------- Redis ----------
REDIS_PASSWORD=RedisPass456

# ---------- Ứng dụng ----------
APP_PORT=9090
CACHE_TTL=60
